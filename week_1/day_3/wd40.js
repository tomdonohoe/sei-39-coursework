var doesItMove = true;
var shouldItMove = false;

if (doesItMove) {
    if (shouldItMove) {
        console.log("No Problem");
    } else {
        console.log("Use some duct tape");
    }
} else {
    if (shouldItMove) {
        console.log("Use WD40");
    } else {
        console.log("No problem");
    }
}